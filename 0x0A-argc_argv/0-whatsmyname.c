#include "main.h"
#include <stdio.h>

/**
 * main - Entry point
 * @argc: argument counter
 * @argv: argument vector
 * Return: 0 on scs
 */
int main(int argc, char *argv[])
{
	(void) argc;
	printf("%s \n", argv[0]);

	return (0);
}
